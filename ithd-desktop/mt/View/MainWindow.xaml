<Window x:Class="mt.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:mt"
        xmlns:control="clr-namespace:mt.UserControls"
        mc:Ignorable="d"
        Title="ITHD Managemennt Tool" Height="450" Width="800">
    <Window.Resources>
        
    </Window.Resources>
    <ScrollViewer ScrollViewer.VerticalScrollBarVisibility="Auto">
        
        <DockPanel>
            <Border Name="brdMain" DockPanel.Dock="Top">
                <control:Login x:Name="Login"/>
            </Border>
            <!--
            <StackPanel>
                <Grid Height="419">
                    <Grid.Background>
                        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                            <GradientStop Color="#FF191919" Offset="0"/>
                            <GradientStop Color="#FF321919" Offset="1"/>
                        </LinearGradientBrush>
                    </Grid.Background>
                    <control:Login x:Name="login"/>
                </Grid>
            </StackPanel>
            -->
        </DockPanel>
    </ScrollViewer>
</Window>
